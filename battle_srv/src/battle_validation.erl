-module(battle_validation).

-include("battle_defs.hrl").

-include_lib("stdlib/include/qlc.hrl").

-ifdef(TEST).
-compile(export_all).
-endif.

-export([
    init/0
    ]).

-export([
    ]).

init() ->
    {ok, []}.

